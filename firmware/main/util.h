#ifndef UTIL_H
#define UTIL_H

#ifndef min
/**
 * Return the lesser of the two values.
 *
 * @param X A number.
 * @param Y A number.
 *
 * @return The smaller value.
 */
#define min(X, Y) ((X) < (Y) ? (X) : (Y))
#endif

#endif
